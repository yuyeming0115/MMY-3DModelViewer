# 2026-07-11 工作日志

## 图标重制 + Windows 打包 + 提交推送（MMY-3DModelViewer）

- 源图标 `素材/icon.png`（2048×2048 RGBA 透明，已做透明/像素切割）通过 `npx tauri icon` 重新生成 `src-tauri/icons` 全套：icon.ico / icon.icns / 32~512 PNG / iOS AppIcon-* / Android mipmap / Windows Store Square*。
- 修正图标源路径引用：`build-windows.ps1`、`build-macos.sh`、`BUILD.md` 中 `FBXView-logo.png` → `素材/icon.png`（旧 `素材/FBXView-*.png` 已删除）。
- 构建：sandbox 禁止 `cmd.exe`（build-windows.bat 走 cmd 且含 pause，PowerShell 工具也拦 cmd），改用 VS2022 Community 的 DevShell 模块 `Enter-VsDevShell -Arch amd64` 注入 MSVC 链接器后执行 `npx tauri build`，再把 `target/release/mmy-3d-model-viewer.exe` 与 `target/release/bundle/nsis/*.exe` 汇总到 `release/`（等效 build-windows.ps1 第5步）。
- 产物：`release/MMY-3DModelViewer_0.1.0_x64-setup.exe`（NSIS 安装包）+ `MMY-3DModelViewer-portable.exe`。注意 `release/`、`target/`、`dist/` 均被 `.gitignore` 忽略，不进 git。
- 提交 `04ee4ac`（84 文件），已 push 到 `origin/main`（`26d47ba..04ee4ac`）。

## 踩坑记
- git 推送：本环境 `http(s).proxy=127.0.0.1:7897`（Clash）**实际可达**，清空代理走直连反而 github:443 超时；保留代理 + `git config http.postBuffer 1048576000` 后大包推送成功（之前 408/断开是代理对大 pack 上传超时）。
